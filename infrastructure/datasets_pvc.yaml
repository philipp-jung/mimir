apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mimir-datasets-volume
  namespace: pjung
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 50Gi
  storageClassName: shared
